# پلن نزدیک‌تر کردن ARSHNAZ به TickTick

## هدف

برنامه در حال حاضر امکانات مشابه TickTick (تسک، تگ، فولدر، تقویم، عادت، پومودورو، یادداشت) را دارد، اما شکل و UX آن متفاوت است. این پلن مرحله‌به‌مرحله برنامه را به لحاظ **شکل (UI/UX)** و **جریان کاری** به TickTick نزدیک می‌کند، بدون اینکه بخش‌های بهداشت روان و CBT که تمایز برنامه هستند حذف شوند.

## وضعیت فعلی در مقایسه با TickTick

| بخش | TickTick | ARSHNAZ فعلی | فاصله‌ی اصلی |
|-----|----------|--------------|---------------|
| **تمرکز اصلی** | تسک و انجام کار | تسک + سلامت روان | تعداد مسیرهای جانبی در home/sidebar زیاد است |
| **نوار پایین** | ۵ تب: Today, Calendar, Search, Timer, Settings | ۴ تب: Today, Calendar, Habits, Mind + دکمه وسط Add | Mind با یک تب کلی قرار گیرد؛ Timer جداگانه یا داخل Focus |
| **لیست تسک‌ها** | تمیز، چک‌باکس چپ، عنوان، ددلاین/تگ، اولویت | اطلاعات زیاد، آیکون‌ها و رنگ‌های متنوع | ساده‌سازی ردیف تسک؛ فقط اطلاعات مهم نمایش داده شود |
| **Quick Add** | همیشه قابل‌دسترس، NLP تاریخ/اولویت/لیست | Quick Add هست ولی کوچک/محدود | افزایش دید، پشتیبانی از `#tag` `@folder` `!priority` در متن |
| **Task Detail** | شیت تمیز با بخش‌های جمع‌شونده | صفحه کامل با آیکون‌های متعدد پایین | ساده‌سازی Rail پایین، ادغام زمان‌بندی/تکرار/یادآور |
| **کالندر** | Month/Week/Day/Agenda با نمایش تسک | چهار view وجود دارد | بهبود رنگ‌بندی و نمایش سریع‌تر تسک روی روزها |
| **عادت‌ها** | جدول هفتگی با streak flame | لیست با ۷ روز اخیر | نمایش تقویم هفتگی/ماهانه و نمودار streak |
| **پومودورو** | تایمر ساده + آماری روز/هفته | تایمر + خلاصه امروز | افزودن نمودار هفتگی و اتصال ساده‌تر به تسک |
| **تم و رنگ** | روشن/سفید، رنگ‌های ملایم، گوشه‌های گرد | dark + gradient + glassmorphism | افزودن **تم TickTick** (light/minimal) به تنظیمات |
| **Search** | جستجوی سریع سراسری | Command Palette هست | افزودن search bar در هدر با نتایج فوری |

## اصول طراحی برای نزدیکی به TickTick

1. **تمرکز روی تسک**: صفحه‌ی پیش‌فرض app باید `Today` یا `Inbox` باشد، نه dashboard پر از widget.
2. **کمتر = بیشتر**: هر ردیف تسک حداکثر چک‌باکس، عنوان، تاریخ، اولویت، یک تگ.
3. **Quick Add همیشه قابل‌دسترس**: نوار ورودی تسک در پایین صفحه یا فلوتی بزرگ.
4. **ناوبری ساده**: sidebar تاشو با فقط Inbox/Today/Next7/Calendar/Projects/Tags/Settings.
5. **بخش‌های بهداشت روان**: در یک تب/مسیر جداگانه‌ی «Mind» نگه داشته شوند، اما در مسیر اصلی تسک دخالت نکنند.
6. **تم روشن**: افزودن یک تم light & minimal شبیه TickTick، با گزینه‌ی بازگشت به تم فعلی.

## رودمپ فازبه‌فاز

### فاز ۱: اسکلت اصلی و ناوبری (۲–۳ روز)

- تغییر **BottomTabBar** به ۵ تب مشابه TickTick:
  - `Today` (لیست تسک)
  - `Calendar`
  - `+` (Quick Add مرکزی بزرگ)
  - `Habits`  → یا `Timer` (تمرکز)
  - `Settings`
- جابه‌جایی Mind/ABC/CBT به داخل منوی `More` یا یک تب مجزای `Mind` (نه در نوار اصلی).
- افزودن **Quick Add Bar** در پایین صفحه‌ی TasksView (نه فقط آیکون + شناور).
- اضافه کردن **Search Bar** در header به جای دکمه جستجو/AI، با shortcut ⌘K.

### فاز ۲: لیست تسک و ردیف تسک (۲–۳ روز)

- بازطراحی **TaskRow**:
  - چک‌باکس گرد در چپ
  - عنوان تسک بدون رنگ غلیظ
  - فقط آیکون‌های کوچک: ددلاین، اولویت (بدون emoji، فقط رنگ)، یک تگ
  - حذف/مخفی کردن توضیحات/تعداد زیرتسک مگر با expand
- افزودن **grouping** در Today: Overdue → Today → Tomorrow → Upcoming.
- ساپورت **swipe right/left** برای Complete / Delete / Postpone.
- ساده‌سازی **Sorting**: Due Date → Priority → Created.

### فاز ۳: Quick Add و Task Detail (۳–۴ روز)

- تقویت **QuickAddTask**:
  - شناسایی `#tag`، `@folder/list`، `!p1` / `!!` برای اولویت
  - نمایش chipهای تجزیه‌شده قبل از submit
  - ارتفاع بیشتر و placeholder شبیه TickTick
- بازطراحی **TaskDetail**:
  - تبدیل Rail پایین به شیت/دکمه‌های ساده
  - ادغام Date + Time + Reminder + Repeat در یک پاپ‌اوور «زمان‌بندی»
  - زیرتسک‌ها و مراحل در یک بخش واحد «Sub-tasks & Checklist»
  - Attachments و Notes در یک بخش «Notes & Files»
- قابلیت ** postpone / snooze** سریع.

### فاز ۴: کالندر، عادت‌ها و پومودورو (۳–۴ روز)

- **CalendarView**:
  - نمایش تسک‌ها به صورت dot/mini-card در روزها
  - دسترسی سریع به تسک‌های روز با tap
  - افزودن Quick Add از داخل کالندری
- **HabitsView**:
  - جدول ۷ روزه/ماهانه با چک‌خانه‌های کوچک
  - نمایش streak و بهترین رکورد
  - نمودار simple completion rate
- **PomodoroView**:
  - تایمر full-screen ساده‌تر
  - انتخاب سریع تسک فعلی
  - نمودار تمرکز هفتگی

### فاز ۵: تم TickTick و جستجو (۲–۳ روز)

- افزودن **theme preset «TickTick Light»** در `SettingsView`:
  - رنگ‌های سفید/خاکستری ملایم
  - آیکون‌های خطی نازک
  - گوشه‌های گرد کمتر
  - حذف gradient/glass در این preset
- بهبود **CommandPalette**:
  - جستجوی لحظه‌ای در تسک‌ها، فولدرها، تگ‌ها
  - دستورات سریع: «today», «inbox», «pomodoro»
- افزودن **Stats/Summary page** خلاصه روز/هفته (انجام‌شده، تمرکز، عادت).

## ملاحظات فنی

- **RTL**: TickTick معمولاً LTR است؛ برای نسخه‌ی فارسی باید چک‌باکس در راست، متن در چپ قرار گیرد (`flex-row-reverse` با کلاس‌های مناسب).
- **دیتابیس**: تغییرات جدولی نیاز نیست؛ فیلدها (folder/tag/priority/due_date) موجودند. ممکن است برای streak/wallet theme یک ستون در `user_settings` اضافه شود.
- **Lovable sync**: هر تغییر UI باید با Lovable sync شود؛ فایل‌های `.tsx` اصلی را در PRهای کوچک و جداگانه ارسال کن تا conflict کمتر شود.
- **Performance**: از `memo` و `virtualized list` برای لیست بلند تسک استفاده شود.

## ریسک‌ها و تصمیمات باز

| موضوع | گزینه‌ها | پیشنهاد |
|-------|----------|---------|
| **حذف dashboard home?** | حذف کامل / جابه‌جایی به More / نگه‌داشتن اختیاری | نگه‌داشتن اختیاری؛ پیش‌فرض Today شود |
| **Mind tab در نوار پایین؟** | حذف / جابه‌جایی به منوی side / نگه‌داشتن | به `More` یا یک تب پنجم «Mind» منتقل شود |
| **تم TickTick به صورت جایگزین یا پیش‌فرض؟** | جایگزین کامل / toggle / theme preset | theme preset جدید؛ پیش‌فرض فعلی حفظ شود |
| **استفاده از NLP انگلیسی؟** | فقط فارسی / هر دو | ابتدا فارسی (فردا، هفته آینده، ساعت ۵) سپس انگلیسی |

## گام‌های بعدی

1. تأیید این پلن و انتخاب فاز شروع (پیشنهاد: فاز ۱ + فاز ۲).
2. اگر تمایل به پیاده‌سازی دارید، اولین PR را با «BottomTabBar + Quick Add Bar + TaskRow ساده‌شده» شروع کنم.
3. در غیر این صورت می‌توانم ماک‌آپ متنی یا wireframe دقیق‌تری برای هر صفحه تهیه کنم.

---

*پلن تهیه‌شده بر اساس بررسی کد: `src/pages/TasksView.tsx`, `src/components/TaskDetail.tsx`, `src/components/AppSidebar.tsx`, `src/components/BottomTabBar.tsx`, `src/pages/HomeView.tsx`, `src/pages/CalendarView.tsx`, `src/pages/HabitsView.tsx`, `src/pages/PomodoroView.tsx`.*
