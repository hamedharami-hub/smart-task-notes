version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arshnaz-app
    restart: unless-stopped
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
